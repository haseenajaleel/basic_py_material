num=[1,4,5,6,7,3]
target=2
def present(num,t):
    for i in range (len(num)):
       for j in range(i+1,len(num)):
            if (num[i]+num[j]==t):
                return([i,j])
    return None
print(present(num,target))